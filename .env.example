# Cloudflare Tunnel Token
# Cloudflare Zero Trust Dashboard > Networks > Tunnels > Create Tunnel
# Token'ı buraya yapıştırın
TUNNEL_TOKEN=

# Tunnel hostname (opsiyonel, CORS ayarları için)
# Örn: coronary.yourdomain.com
TUNNEL_HOSTNAME=
